
name: Build and test

on:
  pull_request:
    branches: [ MC17 ]
  push:
    branches: [ MC17 ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
